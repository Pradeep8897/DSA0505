import pandas as pd

df = pd.read_csv("job_history.csv")

job_count = df.groupby("EMPLOYEE_ID").size()

employees_two_jobs = job_count[job_count >= 2].index

print("Employees who have done two or more jobs:")
print(employees_two_jobs)


output:
Employees who have done two or more jobs:
Index([101, 176, 200], dtype='int64', name='EMPLOYEE_ID')
